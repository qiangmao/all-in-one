# 服务器端口
server.port=8080

# 应用名称
spring.application.name=demo

# 日志配置
logging.level.root=INFO
logging.level.com.example.demo=DEBUG
# GitHub API配置
# 注意：实际使用时，应该将token放在环境变量或配置服务中，而不是直接放在配置文件中
# github.token=your_github_token_here
